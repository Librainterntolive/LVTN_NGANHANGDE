// Địa chỉ API của backend Go (Gin)
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8081/api',
};

// Mẫu cấu hình. Chép file này thành environment.ts (file đó không được theo dõi
// trong git) rồi sửa apiUrl cho khớp nơi backend đang chạy.
